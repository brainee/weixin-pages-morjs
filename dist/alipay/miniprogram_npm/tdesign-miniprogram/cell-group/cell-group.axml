<import-sjs name="_" from="../common/utils.sjs"></import-sjs>

<view class="class {{ classPrefix }}__title {{prefix}}-class-title" a:if="{{ title }}"> {{ title }} </view>
<view style="{{_._style([style, customStyle])}}" class="{{_.cls(classPrefix, [['bordered', bordered], theme])}} class {{prefix}}-class">
  <slot></slot>
</view>
